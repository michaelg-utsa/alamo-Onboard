# Architecture Diagram Source

The diagram `architecture.svg` was hand-authored in SVG.
To regenerate or edit, open the file in any SVG editor (Inkscape, Figma, etc.) or edit the XML directly.

The diagram reflects the component inventory in `docs/SPEC.md`.
